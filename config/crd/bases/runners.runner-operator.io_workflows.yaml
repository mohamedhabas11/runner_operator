---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: workflows.runners.runner-operator.io
spec:
  group: runners.runner-operator.io
  names:
    kind: Workflow
    listKind: WorkflowList
    plural: workflows
    singular: workflow
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Workflow is the Schema for the workflows API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WorkflowSpec defines the desired state of Workflow.
            properties:
              jobs:
                description: |-
                  Jobs defines parallel job groups. Mutually exclusive with Steps.
                  If both are set, Jobs takes precedence and Steps is ignored.
                items:
                  description: JobSpec defines a group of steps that share state and
                    execute together.
                  properties:
                    env:
                      description: Env defines environment variables for all steps
                        in this job.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      type: array
                    gitRepo:
                      description: GitRepo defines a Git repository to clone before
                        executing each step.
                      properties:
                        auth:
                          description: Auth defines authentication for private repositories.
                          properties:
                            secretRef:
                              description: |-
                                SecretRef references a Secret containing Git credentials.
                                  - For ssh: key "ssh-privatekey" (required), "known_hosts" (optional)
                                  - For basicAuth: keys "username" and "password"
                                  - For token: key "token" (used as HTTPS password with empty username)
                              properties:
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                              type: object
                              x-kubernetes-map-type: atomic
                            type:
                              description: |-
                                Type selects the authentication method: "ssh", "basicAuth", or "token".
                                When omitted, the controller auto-detects based on the URL scheme and
                                the keys present in the referenced Secret.
                              enum:
                              - ssh
                              - basicAuth
                              - token
                              type: string
                          required:
                          - secretRef
                          type: object
                        image:
                          description: Image overrides the default Git image (alpine/git:2.47.2)
                            used for cloning.
                          type: string
                        path:
                          description: |-
                            Path within the repository to use as the working directory.
                            Example: "terraform/prod" or "ansible/playbooks".
                          type: string
                        revision:
                          description: |-
                            Revision is the branch, tag, or commit SHA to checkout.
                            Defaults to the remote HEAD if empty.
                          type: string
                        url:
                          description: URL of the Git repository to clone (HTTPS or
                            SSH).
                          minLength: 1
                          type: string
                      required:
                      - url
                      type: object
                    name:
                      description: Name of the job; must be unique within the workflow.
                      minLength: 1
                      type: string
                    needs:
                      description: Needs lists job names that must complete before
                        this job starts.
                      items:
                        type: string
                      type: array
                    sharedVolume:
                      description: SharedVolume defines a volume shared between all
                        steps in this job.
                      properties:
                        emptyDir:
                          description: EmptyDir configures an emptyDir volume.
                          properties:
                            medium:
                              description: |-
                                medium represents what type of storage medium should back this directory.
                                The default is "" which means to use the node's default medium.
                                Must be an empty string (default) or Memory.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir
                              type: string
                            sizeLimit:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                sizeLimit is the total amount of local storage required for this EmptyDir volume.
                                The size limit is also applicable for memory medium.
                                The maximum usage on memory medium EmptyDir would be the minimum value between
                                the SizeLimit specified here and the sum of memory limits of all containers in a pod.
                                The default is nil which means that the limit is undefined.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                          type: object
                        mountPath:
                          default: /workspace
                          description: MountPath is the path to mount the shared volume.
                            Defaults to "/workspace".
                          type: string
                        persistentVolumeClaim:
                          description: PersistentVolumeClaim configures a PVC volume.
                          properties:
                            claimName:
                              description: |-
                                claimName is the name of a PersistentVolumeClaim in the same namespace as the pod using this volume.
                                More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims
                              type: string
                            readOnly:
                              description: |-
                                readOnly Will force the ReadOnly setting in VolumeMounts.
                                Default false.
                              type: boolean
                          required:
                          - claimName
                          type: object
                      type: object
                    steps:
                      description: Steps defines the ordered steps of the job.
                      items:
                        description: WorkflowStep defines a single step in a Workflow.
                        properties:
                          args:
                            description: Args passed to the container entrypoint.
                            items:
                              type: string
                            type: array
                          command:
                            description: Command overrides the container entrypoint.
                            items:
                              type: string
                            type: array
                          dependsOn:
                            description: DependsOn lists step names that must complete
                              before this one runs.
                            items:
                              type: string
                            type: array
                          env:
                            description: Env defines environment variables for this
                              step.
                            items:
                              description: EnvVar represents an environment variable
                                present in a Container.
                              properties:
                                name:
                                  description: |-
                                    Name of the environment variable.
                                    May consist of any printable ASCII characters except '='.
                                  type: string
                                value:
                                  description: |-
                                    Variable references $(VAR_NAME) are expanded
                                    using the previously defined environment variables in the container and
                                    any service environment variables. If a variable cannot be resolved,
                                    the reference in the input string will be unchanged. Double $$ are reduced
                                    to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                    "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                    Escaped references will never be expanded, regardless of whether the variable
                                    exists or not.
                                    Defaults to "".
                                  type: string
                                valueFrom:
                                  description: Source for the environment variable's
                                    value. Cannot be used if value is not empty.
                                  properties:
                                    configMapKeyRef:
                                      description: Selects a key of a ConfigMap.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the ConfigMap
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    fieldRef:
                                      description: |-
                                        Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                        spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                      properties:
                                        apiVersion:
                                          description: Version of the schema the FieldPath
                                            is written in terms of, defaults to "v1".
                                          type: string
                                        fieldPath:
                                          description: Path of the field to select
                                            in the specified API version.
                                          type: string
                                      required:
                                      - fieldPath
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    fileKeyRef:
                                      description: |-
                                        FileKeyRef selects a key of the env file.
                                        Requires the EnvFiles feature gate to be enabled.
                                      properties:
                                        key:
                                          description: |-
                                            The key within the env file. An invalid key will prevent the pod from starting.
                                            The keys defined within a source may consist of any printable ASCII characters except '='.
                                            During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                          type: string
                                        optional:
                                          default: false
                                          description: |-
                                            Specify whether the file or its key must be defined. If the file or key
                                            does not exist, then the env var is not published.
                                            If optional is set to true and the specified key does not exist,
                                            the environment variable will not be set in the Pod's containers.

                                            If optional is set to false and the specified key does not exist,
                                            an error will be returned during Pod creation.
                                          type: boolean
                                        path:
                                          description: |-
                                            The path within the volume from which to select the file.
                                            Must be relative and may not contain the '..' path or start with '..'.
                                          type: string
                                        volumeName:
                                          description: The name of the volume mount
                                            containing the env file.
                                          type: string
                                      required:
                                      - key
                                      - path
                                      - volumeName
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    resourceFieldRef:
                                      description: |-
                                        Selects a resource of the container: only resources limits and requests
                                        (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                      properties:
                                        containerName:
                                          description: 'Container name: required for
                                            volumes, optional for env vars'
                                          type: string
                                        divisor:
                                          anyOf:
                                          - type: integer
                                          - type: string
                                          description: Specifies the output format
                                            of the exposed resources, defaults to
                                            "1"
                                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                          x-kubernetes-int-or-string: true
                                        resource:
                                          description: 'Required: resource to select'
                                          type: string
                                      required:
                                      - resource
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    secretKeyRef:
                                      description: Selects a key of a secret in the
                                        pod's namespace
                                      properties:
                                        key:
                                          description: The key of the secret to select
                                            from.  Must be a valid secret key.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the Secret
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                              required:
                              - name
                              type: object
                            type: array
                          gitRepo:
                            description: |-
                              GitRepo defines a Git repository to clone before executing the step.
                              When set, an init container clones the repo into a shared volume and the
                              main container's working directory is set to the checkout path.
                            properties:
                              auth:
                                description: Auth defines authentication for private
                                  repositories.
                                properties:
                                  secretRef:
                                    description: |-
                                      SecretRef references a Secret containing Git credentials.
                                        - For ssh: key "ssh-privatekey" (required), "known_hosts" (optional)
                                        - For basicAuth: keys "username" and "password"
                                        - For token: key "token" (used as HTTPS password with empty username)
                                    properties:
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  type:
                                    description: |-
                                      Type selects the authentication method: "ssh", "basicAuth", or "token".
                                      When omitted, the controller auto-detects based on the URL scheme and
                                      the keys present in the referenced Secret.
                                    enum:
                                    - ssh
                                    - basicAuth
                                    - token
                                    type: string
                                required:
                                - secretRef
                                type: object
                              image:
                                description: Image overrides the default Git image
                                  (alpine/git:2.47.2) used for cloning.
                                type: string
                              path:
                                description: |-
                                  Path within the repository to use as the working directory.
                                  Example: "terraform/prod" or "ansible/playbooks".
                                type: string
                              revision:
                                description: |-
                                  Revision is the branch, tag, or commit SHA to checkout.
                                  Defaults to the remote HEAD if empty.
                                type: string
                              url:
                                description: URL of the Git repository to clone (HTTPS
                                  or SSH).
                                minLength: 1
                                type: string
                            required:
                            - url
                            type: object
                          image:
                            description: Image is the Docker image to run (inline
                              alternative to RunnerRef).
                            type: string
                          name:
                            description: Name of the step; must be unique within the
                              workflow.
                            minLength: 1
                            type: string
                          retry:
                            description: Retry configures retry behaviour for this
                              step.
                            properties:
                              backoff:
                                description: Backoff defines the retry backoff strategy.
                                properties:
                                  initialDelay:
                                    description: InitialDelay between retry attempts.
                                    format: duration
                                    type: string
                                  maxDelay:
                                    description: MaxDelay caps the delay between retry
                                      attempts.
                                    format: duration
                                    type: string
                                required:
                                - initialDelay
                                type: object
                              maxRetries:
                                description: MaxRetries is the maximum number of retry
                                  attempts.
                                minimum: 0
                                type: integer
                            required:
                            - maxRetries
                            type: object
                          runnerRef:
                            description: |-
                              RunnerRef references a Runner resource to use as a template.
                              Namespace defaults to the workflow's namespace when empty.
                            properties:
                              name:
                                description: Name of the Runner resource.
                                type: string
                              namespace:
                                description: Namespace of the Runner resource. Defaults
                                  to the workflow's namespace.
                                type: string
                            required:
                            - name
                            type: object
                          timeout:
                            description: Timeout for this step.
                            format: duration
                            type: string
                          when:
                            description: |-
                              When is a condition expression that controls whether this step runs.
                              Evaluated after all dependencies complete. Accepted values: "on_success" (default), "on_failure", "always".
                            enum:
                            - on_success
                            - on_failure
                            - always
                            type: string
                        required:
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    when:
                      description: |-
                        When controls whether this job runs based on its dependency results.
                        Accepted values: "on_success" (default), "on_failure", "always".
                      enum:
                      - on_success
                      - on_failure
                      - always
                      type: string
                  required:
                  - name
                  - steps
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              steps:
                description: |-
                  Steps defines the ordered steps of the workflow (flat mode).
                  Mutually exclusive with Jobs. Ignored when Jobs is set.
                  Kept for backward compatibility.
                items:
                  description: WorkflowStep defines a single step in a Workflow.
                  properties:
                    args:
                      description: Args passed to the container entrypoint.
                      items:
                        type: string
                      type: array
                    command:
                      description: Command overrides the container entrypoint.
                      items:
                        type: string
                      type: array
                    dependsOn:
                      description: DependsOn lists step names that must complete before
                        this one runs.
                      items:
                        type: string
                      type: array
                    env:
                      description: Env defines environment variables for this step.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      type: array
                    gitRepo:
                      description: |-
                        GitRepo defines a Git repository to clone before executing the step.
                        When set, an init container clones the repo into a shared volume and the
                        main container's working directory is set to the checkout path.
                      properties:
                        auth:
                          description: Auth defines authentication for private repositories.
                          properties:
                            secretRef:
                              description: |-
                                SecretRef references a Secret containing Git credentials.
                                  - For ssh: key "ssh-privatekey" (required), "known_hosts" (optional)
                                  - For basicAuth: keys "username" and "password"
                                  - For token: key "token" (used as HTTPS password with empty username)
                              properties:
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                              type: object
                              x-kubernetes-map-type: atomic
                            type:
                              description: |-
                                Type selects the authentication method: "ssh", "basicAuth", or "token".
                                When omitted, the controller auto-detects based on the URL scheme and
                                the keys present in the referenced Secret.
                              enum:
                              - ssh
                              - basicAuth
                              - token
                              type: string
                          required:
                          - secretRef
                          type: object
                        image:
                          description: Image overrides the default Git image (alpine/git:2.47.2)
                            used for cloning.
                          type: string
                        path:
                          description: |-
                            Path within the repository to use as the working directory.
                            Example: "terraform/prod" or "ansible/playbooks".
                          type: string
                        revision:
                          description: |-
                            Revision is the branch, tag, or commit SHA to checkout.
                            Defaults to the remote HEAD if empty.
                          type: string
                        url:
                          description: URL of the Git repository to clone (HTTPS or
                            SSH).
                          minLength: 1
                          type: string
                      required:
                      - url
                      type: object
                    image:
                      description: Image is the Docker image to run (inline alternative
                        to RunnerRef).
                      type: string
                    name:
                      description: Name of the step; must be unique within the workflow.
                      minLength: 1
                      type: string
                    retry:
                      description: Retry configures retry behaviour for this step.
                      properties:
                        backoff:
                          description: Backoff defines the retry backoff strategy.
                          properties:
                            initialDelay:
                              description: InitialDelay between retry attempts.
                              format: duration
                              type: string
                            maxDelay:
                              description: MaxDelay caps the delay between retry attempts.
                              format: duration
                              type: string
                          required:
                          - initialDelay
                          type: object
                        maxRetries:
                          description: MaxRetries is the maximum number of retry attempts.
                          minimum: 0
                          type: integer
                      required:
                      - maxRetries
                      type: object
                    runnerRef:
                      description: |-
                        RunnerRef references a Runner resource to use as a template.
                        Namespace defaults to the workflow's namespace when empty.
                      properties:
                        name:
                          description: Name of the Runner resource.
                          type: string
                        namespace:
                          description: Namespace of the Runner resource. Defaults
                            to the workflow's namespace.
                          type: string
                      required:
                      - name
                      type: object
                    timeout:
                      description: Timeout for this step.
                      format: duration
                      type: string
                    when:
                      description: |-
                        When is a condition expression that controls whether this step runs.
                        Evaluated after all dependencies complete. Accepted values: "on_success" (default), "on_failure", "always".
                      enum:
                      - on_success
                      - on_failure
                      - always
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              timeout:
                description: Timeout for the entire workflow execution.
                format: duration
                type: string
            type: object
          status:
            description: WorkflowStatus defines the observed state of Workflow.
            properties:
              completionTime:
                description: CompletionTime is when the workflow finished.
                format: date-time
                type: string
              conditions:
                description: Conditions represent the current state of the Workflow
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              jobStatuses:
                description: JobStatuses tracks the status of each workflow job.
                items:
                  description: JobStatus tracks the execution status of an individual
                    workflow job.
                  properties:
                    completedAt:
                      description: CompletedAt is when the job finished.
                      format: date-time
                      type: string
                    name:
                      description: Name of the job.
                      type: string
                    phase:
                      description: Phase of the job execution.
                      enum:
                      - Pending
                      - Running
                      - Succeeded
                      - Failed
                      - Skipped
                      - Waiting
                      type: string
                    startedAt:
                      description: StartedAt is when the job started.
                      format: date-time
                      type: string
                  required:
                  - name
                  - phase
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the last generation the controller
                  reconciled.
                format: int64
                type: integer
              phase:
                description: Phase is the current lifecycle phase of the workflow.
                enum:
                - Pending
                - Running
                - Succeeded
                - Failed
                - Unknown
                type: string
              startTime:
                description: StartTime is when the workflow started.
                format: date-time
                type: string
              stepStatuses:
                description: StepStatuses tracks the status of each workflow step.
                items:
                  description: StepStatus tracks the execution status of an individual
                    workflow step.
                  properties:
                    completedAt:
                      description: CompletedAt is when the step finished.
                      format: date-time
                      type: string
                    message:
                      description: Message provides details about the step status.
                      type: string
                    name:
                      description: Name of the step.
                      type: string
                    phase:
                      description: Phase of the step execution.
                      enum:
                      - Pending
                      - Running
                      - Succeeded
                      - Failed
                      - Skipped
                      - Waiting
                      type: string
                    retryCount:
                      description: RetryCount is how many times the step has been
                        retried.
                      type: integer
                    startedAt:
                      description: StartedAt is when the step started.
                      format: date-time
                      type: string
                  required:
                  - name
                  - phase
                  type: object
                type: array
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
