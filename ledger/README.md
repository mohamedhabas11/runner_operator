# ledger/

Agent-internal project tracking. Not user-facing.

- `vision.md` — product vision, architecture decisions, session log
- `tasks.md` — task tracking per session
- `builder_prompt.md` — agent onboarding prompt for design patterns

The `claude_feedback.md` at the repo root is temporary external review output.
