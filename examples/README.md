# runner-operator Examples

<!-- markdownlint-disable MD060 -->

See README.md for full documentation.

| Directory | Description |
|-----------|-------------|
| runner/ | Single disposable job patterns |
| workflow/ | Multi-step DAG pipelines |
| eventtrigger/ | Webhook-triggered pipeline definitions |
| advanced/ | Cross-namespace templates, quotas, isolation |

Apply any example with `kubectl apply -f <file.yaml>`.

> For production use, replace placeholder values (`<base64-...>`, URLs, names)
> with your own configuration.

<!-- markdownlint-enable MD060 -->
