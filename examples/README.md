# runner-operator Examples

| Directory | Description |
|-----------|-------------|
| [`runner/`](./runner) | Single disposable job patterns |
| [`workflow/`](./workflow) | Multi-step DAG pipelines |
| [`eventtrigger/`](./eventtrigger) | Webhook-triggered pipeline definitions |
| [`advanced/`](./advanced) | Cross-namespace templates, quotas, isolation |

Apply any example with `kubectl apply -f <file.yaml>`.

> For production use, replace placeholder values (`<base64-...>`, URLs, names)
> with your own configuration.
